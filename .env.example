# Base de datos
DB_HOST=localhost
DB_USER=root
DB_PASS=secret_password
DB_NAME=creador_web_php_puro

# URL pública de la app (sin slash final)
APP_BASE_URL=https://mi-dominio.com

# SMTP / PHPMailer (opcional, recomendado para envío de correos)
SMTP_HOST=smtp.tu-proveedor.com
SMTP_PORT=587
SMTP_USER=usuario_smtp
SMTP_PASS=contraseña_smtp
SMTP_SECURE=tls

# Remitente de los correos
MAIL_FROM=no-reply@mi-dominio.com
MAIL_FROM_NAME="Mi App"
